package be.nn.hiring.junior.fish;

public class FishSimulator {
    /**
     * Initialize the simulation and then simulate for the given number of days.
     *
     * @param initialState contains a list of internal fish timers. For example: "1,3,4,2,6".
     * @param days         the number of days to simulate.
     * @return The total number of fish at the end of the simulation.
     */
    public long simulate(String initialState, int days) {
        return 0;
    }
}
